# Pinned per architecture doc Section 6. Python 3.11+ required.

# --- Scraping ---------------------------------------------------------------
python-jobspy==1.1.80

# --- NLP validation ---------------------------------------------------------
spacy==3.8.2
# spaCy model installed separately:
#   python -m spacy download en_core_web_sm

# --- Embeddings (local, free) -----------------------------------------------
sentence-transformers==3.3.1

# --- LLM (Gemini 2.0 Flash) + Pydantic structuring --------------------------
google-generativeai==0.8.3
instructor==1.7.0
pydantic==2.10.3

# --- Database ---------------------------------------------------------------
sqlalchemy==2.0.36
psycopg[binary]==3.2.3
pgvector==0.3.6
alembic==1.14.0

# --- Resume building --------------------------------------------------------
python-docx==1.1.2
reportlab==4.2.5
# PDF conversion uses LibreOffice headless (system binary, not pip).

# --- Resume endpoint (Layer 6) ----------------------------------------------
# FastAPI serves /resume/{job_id}.pdf|.docx on demand; uvicorn is the ASGI
# server; httpx backs fastapi.testclient.TestClient in unit tests.
fastapi==0.115.6
uvicorn==0.34.0
httpx==0.28.1

# --- Notifications ----------------------------------------------------------
python-telegram-bot==21.7

# --- Reporting (Google Sheets + Docs) ---------------------------------------
gspread==6.1.4
google-api-python-client==2.154.0
google-auth==2.36.0
google-auth-oauthlib==1.2.1

# --- Logging + config -------------------------------------------------------
structlog==24.4.0
pyyaml==6.0.2
python-dotenv==1.0.1

# --- AWS (S3 + CloudWatch + IAM) --------------------------------------------
# boto3:      runtime SDK (Iteration 0.1+ for aws_check, Iteration 2+ for S3)
# watchtower: structlog → CloudWatch handler (Iteration 3+)
boto3==1.35.76
watchtower==3.3.1

# --- Testing ----------------------------------------------------------------
pytest==8.3.4
pytest-asyncio==0.24.0
# moto: mock boto3 in unit tests (CLAUDE.md testing strategy)
moto[s3,cloudwatch,logs]==5.0.22
